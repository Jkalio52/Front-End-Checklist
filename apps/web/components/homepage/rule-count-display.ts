/** Rounded public rule count used in homepage marketing copy. */
export const PUBLIC_RULE_COUNT_BASE = 350

/** Display label for the rounded public rule count. */
export const PUBLIC_RULE_COUNT_LABEL = `${PUBLIC_RULE_COUNT_BASE}+`
