{
  "name": "@repo/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./src/public-api.ts",
  "types": "./src/public-api.ts",
  "scripts": {
    "lint": "biome check .",
    "test": "jest --passWithNoTests",
    "test:ci": "jest --ci --passWithNoTests",
    "test:coverage": "jest --coverage --ci --passWithNoTests"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "jest": "catalog:",
    "typescript": "catalog:"
  }
}
