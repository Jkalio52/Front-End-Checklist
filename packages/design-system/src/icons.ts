/**
 * Re-export all Lucide icons and the LucideIcon type so the rest of the repo
 * can import icons from @repo/design-system instead of lucide-react directly.
 */

export type { LucideIcon } from 'lucide-react'
export * from 'lucide-react'
