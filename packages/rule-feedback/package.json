{
  "name": "@repo/rule-feedback",
  "version": "0.0.1",
  "private": true,
  "description": "Domain logic for rule feedback and credibility scoring",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "biome check .",
    "test": "jest --watchman=false --passWithNoTests",
    "test:ci": "jest --watchman=false --ci --passWithNoTests",
    "test:coverage": "jest --watchman=false --coverage --ci --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/types": "workspace:*"
  },
  "devDependencies": {
    "@repo/config-typescript": "workspace:^",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "jest": "catalog:",
    "typescript": "catalog:"
  }
}
